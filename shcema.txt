1. Start up bottom features
3. convention return of services (controller class)
4. joi to validate services' paramaters


user{name, password, email, adderess[], phone, role, company }

product{name, category, price, color_size, specification{}, sellerId, description, //avgRate, noInStock, noSold, genaralDiscount, views} //avg rate updated for every review insert

--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
review{userId, productID, message, date, noOfStars}

cart(userId, [product{id, color, size, count}]) //check if product, color, size, count: avaliable

order(userId, [product{id, color, size, count}]), discount, address, status, productsPrice, taxPrice, deliveryPrice, paymentType, StripePaymentId(required if type=credit card), OrderedAt, deliveredAt)
										status_enum: ['Not Processed', 'Processing', 'Shipped', 'Delivered', 'Cancelled']	
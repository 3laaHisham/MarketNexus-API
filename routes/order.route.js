const express = require("express");
const router = express.Router();

// const { method1, method2 } = orderController;

// router.route("/:id").post(protect, method1).get(protect, method2);

module.exports = router;
